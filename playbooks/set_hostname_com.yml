---
- name: Example hostname update to netfusiontechnologies.com
  hosts: managed_linux
  become: true
  gather_facts: true

  tasks:
    - name: Example placeholder
      ansible.builtin.debug:
        msg: "Use the site repo playbook for actual hostname enforcement."
